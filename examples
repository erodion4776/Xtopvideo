// ============================================
// HTML TEMPLATE EXAMPLES
// Users can load these to see how to structure their HTML
// ============================================
const EXAMPLES = {
    memorial: `<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            margin: 0;
            background: radial-gradient(circle at center, #2e2010 0%, #0a0806 80%, #000 100%);
            color: white;
            font-family: 'Georgia', serif;
            width: 100vw;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            padding: 60px 40px;
            box-sizing: border-box;
            overflow: hidden;
        }
        h1 {
            color: #d4af37;
            font-size: 68px;
            font-weight: normal;
            letter-spacing: 3px;
            text-shadow: 0 4px 20px rgba(0,0,0,0.8);
            margin: 0;
        }
        .subtitle {
            color: #bbb;
            font-size: 24px;
            letter-spacing: 5px;
            text-transform: uppercase;
            margin-top: 10px;
        }
        #caption {
            background: rgba(0,0,0,0.75);
            color: white;
            padding: 20px 40px;
            border-radius: 8px;
            font-size: 42px;
            line-height: 1.4;
            text-align: center;
            max-width: 85%;
            opacity: 0;
            transition: opacity 0.4s;
            text-shadow: 2px 2px 8px rgba(0,0,0,0.9);
            border: 1px solid rgba(212, 175, 55, 0.2);
        }
        .header { text-align: center; }
    </style>
</head>
<body>
    <div class="header">
        <h1>In Loving Memory</h1>
        <div class="subtitle">Forever In Our Hearts</div>
    </div>

    <div id="caption"></div>

    <div style="height:20px;"></div>

    <script>
        const captions = [
            { start: 0, end: 3, text: "We remember you" },
            { start: 3, end: 6, text: "Today, Dad" },
            { start: 6, end: 9, text: "With every breath we take" },
            { start: 9, end: 12, text: "Your love still holds us close" },
            { start: 12, end: 15, text: "Forever in our hearts" }
        ];

        function updateCaption(time) {
            const el = document.getElementById('caption');
            const active = captions.find(c => time >= c.start && time <= c.end);
            if (active) {
                el.textContent = active.text;
                el.style.opacity = '1';
            } else {
                el.style.opacity = '0';
            }
        }

        // Expose function for the video engine
        window.__updateAtTime = updateCaption;
    </script>
</body>
</html>`,

    karaoke: `<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            margin: 0;
            background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
            color: white;
            font-family: 'Arial Black', sans-serif;
            width: 100vw;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        #caption {
            text-align: center;
            font-size: 72px;
            font-weight: 900;
            line-height: 1.3;
            max-width: 85%;
            text-shadow: 0 0 30px rgba(0,0,0,0.9);
        }
        .word {
            display: inline-block;
            margin: 0 8px;
            color: white;
            transition: all 0.2s;
        }
        .word.active {
            color: #ffe600;
            transform: scale(1.2);
            text-shadow: 0 0 40px rgba(255,230,0,0.9), 0 0 60px rgba(255,230,0,0.5);
        }
    </style>
</head>
<body>
    <div id="caption"></div>

    <script>
        const captions = [
            { start: 0, end: 4, text: "Turn your dreams into reality" },
            { start: 4, end: 8, text: "Never give up on yourself" },
            { start: 8, end: 12, text: "Rise above every challenge" }
        ];

        function updateCaption(time) {
            const el = document.getElementById('caption');
            const active = captions.find(c => time >= c.start && time <= c.end);
            if (!active) { el.innerHTML = ''; return; }

            const words = active.text.split(' ');
            const wordDur = (active.end - active.start) / words.length;
            const idx = Math.min(Math.floor((time - active.start) / wordDur), words.length - 1);

            el.innerHTML = words.map((w, i) => 
                \`<span class="word \${i <= idx ? 'active' : ''}">\${w}</span>\`
            ).join('');
        }

        window.__updateAtTime = updateCaption;
    </script>
</body>
</html>`,

    tiktok: `<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            margin: 0;
            background: linear-gradient(135deg, #ff0080, #7928ca, #ff0080);
            background-size: 400% 400%;
            animation: bg 8s ease infinite;
            color: white;
            font-family: 'Impact', sans-serif;
            width: 100vw;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        @keyframes bg {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }
        #caption {
            font-size: 90px;
            font-weight: 900;
            text-align: center;
            max-width: 90%;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-shadow: 
                -3px -3px 0 #000,
                3px -3px 0 #000,
                -3px 3px 0 #000,
                3px 3px 0 #000,
                0 8px 30px rgba(0,0,0,0.8);
            transform: scale(0);
            transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        #caption.visible { transform: scale(1); }
        .badge {
            position: absolute;
            top: 40px;
            background: rgba(0,0,0,0.5);
            padding: 12px 24px;
            border-radius: 30px;
            font-size: 22px;
            backdrop-filter: blur(10px);
        }
    </style>
</head>
<body>
    <div class="badge">@yourhandle</div>
    <div id="caption"></div>

    <script>
        const captions = [
            { start: 0, end: 2, text: "WAIT FOR IT" },
            { start: 2, end: 4, text: "THIS IS INSANE" },
            { start: 4, end: 6, text: "FOLLOW FOR MORE" }
        ];

        function updateCaption(time) {
            const el = document.getElementById('caption');
            const active = captions.find(c => time >= c.start && time <= c.end);
            if (active) {
                el.textContent = active.text;
                el.classList.add('visible');
            } else {
                el.classList.remove('visible');
            }
        }

        window.__updateAtTime = updateCaption;
    </script>
</body>
</html>`,

    quote: `<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            margin: 0;
            background: linear-gradient(135deg, #141e30, #243b55);
            color: white;
            font-family: 'Georgia', serif;
            width: 100vw;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            padding: 40px;
            box-sizing: border-box;
        }
        .quote-mark {
            font-size: 200px;
            color: #ffd700;
            line-height: 1;
            opacity: 0.3;
        }
        #caption {
            font-size: 52px;
            line-height: 1.5;
            font-style: italic;
            text-align: center;
            max-width: 900px;
            opacity: 0;
            transition: opacity 0.5s;
        }
        .author {
            margin-top: 40px;
            font-size: 28px;
            color: #ffd700;
            letter-spacing: 3px;
        }
    </style>
</head>
<body>
    <div class="quote-mark">"</div>
    <div id="caption"></div>
    <div class="author">— Author Name</div>

    <script>
        const captions = [
            { start: 0, end: 4, text: "The only way to do great work is to love what you do." },
            { start: 4, end: 8, text: "If you haven't found it yet, keep looking." },
            { start: 8, end: 12, text: "Don't settle." }
        ];

        function updateCaption(time) {
            const el = document.getElementById('caption');
            const active = captions.find(c => time >= c.start && time <= c.end);
            if (active) {
                el.textContent = active.text;
                el.style.opacity = '1';
            } else {
                el.style.opacity = '0';
            }
        }

        window.__updateAtTime = updateCaption;
    </script>
</body>
</html>`,

    podcast: `<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            margin: 0;
            background: #1a1a2e;
            color: white;
            font-family: 'Helvetica', sans-serif;
            width: 100vw;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            padding: 60px;
            box-sizing: border-box;
            overflow: hidden;
        }
        .show-name {
            font-size: 24px;
            color: #00d9ff;
            letter-spacing: 5px;
            text-transform: uppercase;
        }
        #caption {
            font-size: 48px;
            font-weight: 300;
            line-height: 1.5;
            text-align: center;
            max-width: 90%;
            opacity: 0;
            transition: opacity 0.4s;
        }
        .footer {
            display: flex;
            align-items: center;
            gap: 20px;
        }
        .dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #00d9ff;
            animation: pulse 1.5s infinite;
        }
        @keyframes pulse {
            0%, 100% { opacity: 1; transform: scale(1); }
            50% { opacity: 0.5; transform: scale(1.3); }
        }
        .handle { font-size: 20px; color: #888; }
    </style>
</head>
<body>
    <div class="show-name">The Podcast</div>
    <div id="caption"></div>
    <div class="footer">
        <div class="dot"></div>
        <div class="handle">@podcastname</div>
    </div>

    <script>
        const captions = [
            { start: 0, end: 4, text: "Success isn't about being the smartest person in the room." },
            { start: 4, end: 8, text: "It's about being the most persistent." },
            { start: 8, end: 12, text: "Keep showing up, every single day." }
        ];

        function updateCaption(time) {
            const el = document.getElementById('caption');
            const active = captions.find(c => time >= c.start && time <= c.end);
            if (active) {
                el.textContent = active.text;
                el.style.opacity = '1';
            } else {
                el.style.opacity = '0';
            }
        }

        window.__updateAtTime = updateCaption;
    </script>
</body>
</html>`
};
